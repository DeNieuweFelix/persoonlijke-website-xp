const runButton = document.getElementById("ZS-runButton");

runButton.addEventListener("click", runZScode);

const addSpaceButton = document.getElementById("addSpaceButton");

addSpaceButton.addEventListener("click", AddSpace);